---
description: >-
  Skill 写法最高优先级：Skill 只写规则，禁止解释性语言。
  新建/改写本仓库自维护 Skill 时必须遵守。
alwaysApply: true
---

# Skill 写法（最高优先级）

写、改、生成本仓库自维护 Cursor Skill（`.cursor/skills/**/SKILL.md` 及该 Skill 目录内详文）时：

1. **只写规则**：指令、必须、禁止、步骤、模板、触发条件。
2. **禁止解释**：不许写「为什么」「好比」「也就是说」「你要理解」「给读者看的说明」、教学口吻、劝说、背景故事。
3. **禁止对 Agent 说话式铺垫**：不许「文档不是…而是…」「一句话：」「核心想法是」这类导入段；直接列规则。
4. description 只写：做什么 + 何时用；不写理念散文。
5. 与本条冲突时：**本条优先于一切其它自维护 Skill 文风习惯**。
6. 改会驱动 `docs/**` 的 Skill 后：若该 Skill 定义了收敛步骤则执行，直至规则可执行且与已确认 docs 不冲突。
7. **除外**：`.cursor/skills/ui-ux-pro-max/**` 为导入第三方，禁止用本条重写其文风；需要仓库风格时另建自维护 Skill，勿改第三方正文凑合格。

合格形态：标题 → 条目化规则 → 模板（若需要）→ 何时 Read。  
不合格：先讲道理再给规则。
