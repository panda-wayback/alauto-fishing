---
description: >-
  方案先行、何时 Read Skill、读代码路径。
  写/改 Skill 另遵 skill-writing.mdc。
alwaysApply: true
---

# 项目工作流硬约束

## 需求与听从（最高优先级）

1. 先确认用户要的结果（不是手段），再动手。
2. 直接找能达成该结果的方法；禁止改与目标无关的东西。
3. 用户指定的做法照做；有异议先说，未同意不得替换。
4. 未达成不得说完成；无法验证就说未验证。
5. 两次没达成 → 停，重新找方法，不在原路修补。

写/改 `.cursor/skills/**` → 先遵 `skill-writing.mdc`。

## 方案先行

```text
讨论 → 确认 → 写 docs/（方案完备）→ 再按 docs 写代码
```

1. 新功能 / 改产品逻辑或架构：禁止先写代码；先讨论并确认后写入 `docs/`。
2. **架构契约**（角色分工、主题/订阅关系、状态机、版本与作废、谁可订谁不可订）必须先写进对应 `docs/` 能力页并确认完备，禁止边写薄文档边写实现。
3. 实现以当前 `docs/` 为准；禁止未讨论就改已定方案。
4. 实现中发现方案问题 → 先讨论 → `改 docs → 改代码`；禁止偷偷改设计。
5. 可直接改代码：Bug / 重构 / 性能 / 测试 / 不影响方案与契约的实现调整。
6. `方案问题 → docs`；`实现问题 → code`。保持简单，不堆多余流程。
7. 文档必须合理；禁止不合理或未确认内容入档。判定不合理 → 坚持合理做法，禁止迎合。

## 何时 Read Skill

### `doc-node-graph`

探讨方案、写/改 `docs/**`、选下一步、docs 收敛 → Read `.cursor/skills/doc-node-graph/SKILL.md`。  
产品正文只写在 `docs/`，禁止写进该 Skill。

### `ai-friendly-codebase`

写/审 `AI.md`、按索引找代码、判上层是否过厚 → Read `.cursor/skills/ai-friendly-codebase/SKILL.md`。

## 读 / 改代码

```text
docs/（方案）→ 根 AI.md 全树 → 叶子 AI.md → 实现文件
```

- 禁止无目标通读代码树。
- 根 `AI.md` 全树只列目录（或入口文件）+ 一句话；契约只写在叶子 `AI.md`。
- 找/改某包代码前：先 Read 根全树对应行与该包叶子 `AI.md`；契约足够则禁止再遍历该目录。
- 改完且测试通过 → 更新相关 `AI.md`；新目录补叶子并在根全树加一行。
- `docs/` = 方案；`AI.md` = 代码索引；行为以代码 + 测试 + docs 为准。
