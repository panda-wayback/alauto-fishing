# 识别（Detect）

## 目标

不断消费截图段产生的**最新**画面，识别鱼漂位置并记录/发布结果。本段不做找条、截图、决策、点击。

## 要实现的

- 输入：最新 Frame（无新帧则等待）。
- 手段：色块（HSV 绿条 + 空洞/缺绿等，与 AutoFishing 对齐）；可读数平滑。
- 输出：鱼漂 **0～100**（及识别是否有效）；供算法段订阅。
- 只处理比上次更新的帧；错 ROI 版本的帧丢弃（约定见流水线）。
- 不做：圈定范围、mss、判定按住/松开、键鼠执行。

## 解决步骤

待拆
