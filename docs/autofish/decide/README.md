# 算法判定（Decide）

## 目标

只根据识别结果判定下一步操作意图（例如按住收线 / 松开）。本段不截屏、不识图实现细节、不直接点鼠标。

## 要实现的

- 输入：识别段发布的 Pos / 钓鱼状态（及约定观测字段）。
- 输出：动作意图（与模拟器侧「是否按住」同语义，便于共用策略思路）。
- 可与 [`docs/simulator/algo-test/`](../../simulator/algo-test/) 的策略接口对齐；真机与模拟共用「观测 → 意图」。
- 不做：mss、CV 找条、色块实现、键鼠落点。

## 解决步骤

待拆
