# 截图（Capture）

## 目标

mss **只**负责按已圈定范围持续出帧。本段不做找条、读数、决策、点击。

## 要实现的

- 有有效 ROI（点1、点2）→ 按该范围持续截图，发布/更新最新 Frame。
- **无有效 ROI → 不截图、不持续监控。**
- 可对外提供主屏/全屏一帧供 Locate 找条（一次性输入，不是本段「持续监控」）。
- 只关心当前范围与出帧；ROI 变更后旧帧由流水线作废约定处理。
- 不做：模板找条、色块识别、算法、键鼠。

## 解决步骤

待拆
